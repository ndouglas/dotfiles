#!/usr/bin/env bash

# Get the secondary color for the hostname.
nd_get_hostname_secondary_color() {
  : "${1?"Usage: ${FUNCNAME[0]} HOSTNAME"}";
  the_hostname="${1}";
  case "${the_hostname}" in
  {% for item in hostvars %}
    {{ item }}*) color="$(tput setaf {{ hostvars[item].prompt.colors.secondary }})";;
  {% endfor %}
    *)           color="$(tput setaf 2)";;
  esac
  echo "${color}";
}
