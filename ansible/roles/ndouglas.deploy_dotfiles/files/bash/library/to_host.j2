#!/usr/bin/env bash

{% for item in hostvars %}
nd_to_{{ item }}() {
  {% if item == ansible_hostname %}
  "${@}";
  {% else %}
  ssh {{item}} -tt 'bash -l -c '"${@}; bash;"';
  {% endif %}
}
{% endfor %}
